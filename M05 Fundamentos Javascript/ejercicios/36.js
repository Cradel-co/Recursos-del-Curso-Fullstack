// ⛔️ Recuerda que debes utilizar el objeto global "Math".

function elevar(num, exponent) {
  // Tu código:
  return Math.pow(num, exponent);
}

module.exports = elevar;
/**
 si queremos calcular donde 2 es la base y 3 es el exponente, usaríamos Math.pow(2, 3), lo que nos daría como resultado 8.

La función Math.pow() también puede manejar exponentes fraccionarios y negativos. Por ejemplo, Math.pow(4, 0.5) calcularía
 la raíz cuadrada de 4, devolviendo 2.
 */